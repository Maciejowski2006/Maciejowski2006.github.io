{
  "name": "maciejowski2006.github.io",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "compile": "tsc -w && sass -w src/styles/scss:src/styles/css",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.49.0",
    "vite": "^2.7.2"
  },
  "dependencies": {
    "yarn": "^1.22.17"
  }
}
